// mcp-atlassian: Atlassian MCP server (v1 + v2) — Jira, Confluence, Bitbucket
// Provides 27 MCP tools for Atlassian integration

dependencies {
    implementation(project(":modules:mcp-common"))
    implementation(project(":modules:search-engine"))
}
