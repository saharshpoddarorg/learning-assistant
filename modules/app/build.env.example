# =============================================================================
# build.env.example — Local Build Environment Template
# =============================================================================
# COPY THIS FILE to: build.env.local
#   Windows:    copy build.env.example build.env.local
#   Linux/Mac:  cp  build.env.example  build.env.local
#
# build.env.local is GITIGNORED — it contains your machine-specific paths.
# This example file is safe to commit (it contains only placeholder values).
#
# HOW IT WORKS:
#   build.ps1 and build.sh read build.env.local before searching for javac.
#   Values in build.env.local OVERRIDE any same-named environment variables,
#   so you can fix a stale system JAVA_HOME without changing system settings.
#
# ALTERNATIVE:
#   Set JAVA_HOME as a system/user environment variable. The build scripts
#   check JAVA_HOME (from whichever source) before trying fallbacks.
# =============================================================================

# Path to your JDK 17+ installation directory (must contain bin/javac).
# Windows examples:
#   JAVA_HOME=C:\Program Files\Eclipse Adoptium\jdk-21.0.5.11-hotspot
#   JAVA_HOME=C:\Program Files\Microsoft\jdk-21.0.5.11-hotspot
#   JAVA_HOME=E:\mgcnoscan\JDKs\jdk-21.0.7
# Linux/macOS examples:
#   JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
#   JAVA_HOME=/opt/homebrew/opt/openjdk@21

JAVA_HOME=C:\path\to\your\jdk-21
