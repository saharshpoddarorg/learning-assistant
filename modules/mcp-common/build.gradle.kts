// mcp-common: Shared MCP infrastructure — config system, utilities, base server classes
// Used by: mcp-learning-resources, mcp-atlassian, app

dependencies {
    implementation(project(":modules:search-engine"))
}
