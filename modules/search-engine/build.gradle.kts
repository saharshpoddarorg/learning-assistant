// search-engine: Generic, pluggable search library — zero external dependencies
// Used by: mcp-common, mcp-learning-resources, mcp-atlassian

dependencies {
    // No external dependencies — pure Java library
}
